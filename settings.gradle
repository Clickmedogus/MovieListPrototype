pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'https://s3.amazonaws.com/aws-sdk-android-ivy-repo/release' }
    }
}
rootProject.name = "MovieListPrototype"
include ':app'
include ':detail'
include ':video'
